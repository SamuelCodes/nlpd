#!/bin/bash

start_server(){
  gunicorn -w 4 -b 0.0.0.0:8000 nlp.server:wsgiapp
}

console(){
  ipython -i -c "import nlp; from nlp import *"
}

case $1 in
  start)
    start_server
    ;;
  console)
    console
    ;;
  *)
    eval "${*}"
    ;;
esac

